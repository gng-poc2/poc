pull_request_rules:
  - name: dispatch-on-any-pr
    conditions:
      - base = main
      - label = fire
    actions:
      github_actions:
        workflow:
          dispatch:
            - workflow: mgf-priv.yml
