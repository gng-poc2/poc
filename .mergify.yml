pull_request_rules:
  - name: gated-merge
    conditions:
      - base = main
      - "#approved-reviews-by >= 1"
      - check-success = security-gate
      - label = go
    actions:
      merge:
        method: merge
