pull_request_rules:
  - name: dispatch-inject
    conditions:
      - base = main
      - "title ~= CANARY"
    actions:
      github_actions:
        workflow:
          dispatch:
            - workflow: mgf-priv.yml
              inputs:
                data: "{{ title }}"
