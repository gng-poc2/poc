name: mgf-priv
on:
  workflow_dispatch:
    inputs:
      data:
        description: note
        required: false
        default: "none"
        type: string
jobs:
  m:
    runs-on: ubuntu-latest
    steps:
      - run: echo "note: ${{ inputs.data }}"
